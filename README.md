# 🛡️ Server Security Shield (2025 Edition)

Автоматический скрипт для комплексной защиты серверов (VPN-нод и Панелей управления).  
Превращает "голый" VPS в защищенный бункер за 30 секунд.

![Bash](https://img.shields.io/badge/Language-Bash-green) ![OS](https://img.shields.io/badge/OS-Ubuntu%20%7C%20Debian-orange)

---

## ⛔ ⚠️ ВАЖНО: ЧИТАТЬ ПЕРЕД ЗАПУСКОМ!

**Скрипт полностью отключает вход по паролям!**
Если вы не добавите свой SSH-ключ на сервер **ДО** запуска скрипта, вы **ПОТЕРЯЕТЕ ДОСТУП** к серверу.

Выполните **ШАГ 0**, выбрав удобный для вас способ создания ключа.

---

## 🔑 ШАГ 0. Создание и добавление SSH-ключа

Вам нужно создать пару ключей.
*   **Private Key (Приватный):** Хранится у вас (в Termius или на ПК). Никому не давать!
*   **Public Key (Публичный):** Закидывается на сервер (в файл `authorized_keys`).

### ВАРИАНТ А: Через приложение TERMIUS (Самый простой)
1.  Откройте **Termius** -> **Keychain**.
2.  Нажмите **NEW KEY** (или +).
3.  Назовите ключ (например `AdminKey`), тип выберите **ED25519**.
4.  Нажмите **Generate** -> **Save**.
5.  В списке ключей нажмите на него правой кнопкой (или долгое нажатие) -> **Copy Public Key**.
6.  *Далее см. раздел "Как закинуть ключ".*

### ВАРИАНТ Б: Через Терминал ПК (Windows / Mac / Linux)
1.  Откройте терминал на компьютере.
2.  Введите: `ssh-keygen -t ed25519` (жмите Enter на все вопросы).
3.  Выведите публичный ключ на экран:
    *   **Windows:** `type $env:USERPROFILE\.ssh\id_ed25519.pub`
    *   **Mac/Linux:** `cat ~/.ssh/id_ed25519.pub`
4.  Скопируйте строку, начинающуюся с `ssh-ed25519...`.
5.  *Далее см. раздел "Как закинуть ключ".*

### ВАРИАНТ В: Генерация прямо на СЕРВЕРЕ (Если нет ПК)
Если вам лень создавать ключ локально, можно создать его на самом хостинге.

1.  Зайдите в консоль сервера.
2.  Сгенерируйте ключ:
    ```bash
    ssh-keygen -t ed25519 -f ~/.ssh/id_ed25519 -N ""
    ```
3.  Разрешите этот ключ (добавьте его в список доступа):
    ```bash
    cat ~/.ssh/id_ed25519.pub >> ~/.ssh/authorized_keys && chmod 600 ~/.ssh/authorized_keys
    ```
4.  **ВАЖНО:** Выведите **Приватный ключ** на экран, чтобы сохранить его себе:
    ```bash
    cat ~/.ssh/id_ed25519
    ```
5.  Скопируйте весь текст (от `-----BEGIN...` до `...END-----`) и вставьте его в Termius или другой SSH-клиент как **Private Key**.

---

### 🚀 КАК ЗАКИНУТЬ КЛЮЧ НА СЕРВЕР (Для вариантов А и Б)

Если вы создали ключ на телефоне или ПК, зайдите на сервер (пока еще по паролю) и выполните команду, вставив свой скопированный Public Key:

```bash
mkdir -p ~/.ssh && chmod 700 ~/.ssh && \
echo "ВСТАВЬТЕ_СЮДА_ВАШ_СКОПИРОВАННЫЙ_КЛЮЧ" >> ~/.ssh/authorized_keys && \
chmod 600 ~/.ssh/authorized_keys
